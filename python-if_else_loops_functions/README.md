# Python – if/else, loops, functions

This project introduces **control flow and functions in Python**.  
It focuses on using conditional statements, loops, and simple functions while following Python 3 coding standards.

All scripts are written in **Python 3.8** and follow **pycodestyle (PEP 8)** guidelines.

---

## Learning Objectives

At the end of this project, you should be able to explain, without the help of Google:

### General
- Why Python programming is awesome
- Why indentation is so important in Python
- How to use `if`, `if ... else`, and `elif` statements
- How to use `while` and `for` loops
- How to use `break`, `continue`, and `pass`
- How to define and use functions
- What a function returns
- The scope of variables
- What a traceback is
- What logical operators are and how to use them

---

## Requirements

### Python Scripts
- Allowed editors: `vi`, `vim`, `emacs`
- All files are interpreted/compiled on **Ubuntu 20.04 LTS**
- All files use **Python 3.8**
- All files end with a **new line**
- The first line of all files is exactly:
  ```bash
  #!/usr/bin/python3
